#include "main.h"
#include <stdio.h>

/**
 * _abs - print abs value
 * @c: int argument of function
 * Return: 0
 */

int _abs(int c)
{
	if (c > 0 || c == 0)

	{
		return (c);
	}
	else

		return (c * -1);
}
