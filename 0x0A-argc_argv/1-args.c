#include <stdlib.h>
#include <stdio.h>
/**
 * main - prints the number of argument
 * @argc: argument count
 * @argv: argument array
 * Return: Always 0.
 */

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
